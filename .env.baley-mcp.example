# Copying this file is optional. scripts/prepare-local-pilot-agent.ps1 writes
# .env.baley-mcp.local after issuing and verifying a Workspace-scoped token.
BALEY_SERVER_URL=http://127.0.0.1:8080
BALEY_AGENT_TOKEN=replace-with-an-issued-local-token
