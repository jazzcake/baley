[CmdletBinding()]
param()

Set-StrictMode -Version Latest
$ErrorActionPreference = "Stop"

throw "prepare-local-pilot-agent.ps1 is retired. Install the tokenless stdio MCP registration and use the signed-in Workspace connection flow in docs/streamable-http-mcp-operations.md."
